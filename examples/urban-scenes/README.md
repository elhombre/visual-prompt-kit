# Urban Scenes Example

This example generates cinematic city-scene prompts from a reusable meta prompt and a flat parameter catalog.

Try prompt generation:

```bash
vpk prompt --project ./examples/urban-scenes --set subject="a night market courier"
```

Try rendering:

```bash
vpk render --project ./examples/urban-scenes --output ./runs --count 2 --images 2
```
