#!/usr/bin/env node
import { dirname, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'

const currentDir = dirname(fileURLToPath(import.meta.url))
const cliEntry = resolve(currentDir, '../dist/cli/index.js')

await import(cliEntry)
